@html.extend('shared/_layout', function(model){

    @html.block('content', function(model){

        @{
            model.title = "Leaderboard";
        }
        
        <h1>Leaderboard</h1>

    })
})